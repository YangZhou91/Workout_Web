---
// Friendly zero-state shown when the log has no dated workouts yet (FR-013).
---

<div class="empty">
  <div class="emoji">🏋️</div>
  <p>还没有训练记录。</p>
  <p>在 <code>public/training-log.md</code> 的「📅 训练记录」下添加一条 <code>### YYYY-MM-DD（周X）✅ 类型</code> 即可。</p>
</div>
